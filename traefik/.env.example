# Traefik stack env (for /opt/traefik/.env)
#
# Ці змінні потрібні лише для traefik контейнера (DNS-01/ACME, Docker API version).
# Вони не повинні бути в `/opt/powerbot/.env` або `/opt/powerbot-test/.env`.
#
# Приклад використання на сервері:
#   ssh workspace-docker "nano /opt/traefik/.env"
#   ssh workspace-docker "docker compose -f /opt/traefik/docker-compose.yml up -d --force-recreate"

TRAEFIK_EMAIL="admin@example.com"

# Для DNS-01 (Cloudflare). Для HTTP-01 може бути не потрібно.
TRAEFIK_DNS_PROVIDER="cloudflare"
CF_DNS_API_TOKEN=""

# Потрібно, бо Docker daemon може вимагати сучасний API.
TRAEFIK_DOCKER_API_VERSION="1.52"

